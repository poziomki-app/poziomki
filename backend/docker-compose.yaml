services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: loco
      POSTGRES_PASSWORD: loco
      POSTGRES_DB: poziomki-backend_development
    volumes:
      - postgres_data:/var/lib/postgresql/data

  tuwunel:
    image: jevolk/tuwunel:latest
    ports:
      - "6167:6167"
    environment:
      TUWUNEL_SERVER_NAME: "localhost"
      TUWUNEL_PORT: "6167"
      TUWUNEL_ADDRESS: "0.0.0.0"
      TUWUNEL_DATABASE_PATH: "/var/lib/tuwunel"
      TUWUNEL_ALLOW_REGISTRATION: "true"
      TUWUNEL_REGISTRATION_TOKEN: "poziomki-dev-token"
      TUWUNEL_ALLOW_FEDERATION: "false"
      TUWUNEL_CREATE_ADMIN_ROOM: "true"
      TUWUNEL_GRANT_ADMIN_TO_FIRST_USER: "true"
      TUWUNEL_LOG: "info"
    volumes:
      - tuwunel_data:/var/lib/tuwunel
    restart: unless-stopped

volumes:
  postgres_data:
  tuwunel_data:
